# .env
# This file should NOT be committed to Git!

# Django Secret Key (Generate a new one for production!)
DJANGO_SECRET_KEY='your_super_secret_django_key_here_replace_this'

# Debug Mode
# Set to False in production!
DJANGO_DEBUG=True

# Allowed Hosts (comma-separated, no spaces)
DJANGO_ALLOWED_HOSTS=127.0.0.1,localhost

# Database Credentials (No longer needed for SQLite3 - keep this section commented or remove it)
# DB_NAME=task_manager_db
# DB_USER=your_db_user
# DB_PASSWORD=your_db_password
# DB_HOST=localhost
# DB_PORT=5432

# Email Settings
# Set EMAIL_BACKEND to 'django.core.mail.backends.console.EmailBackend' for local testing
# to see emails printed in your console.
EMAIL_BACKEND='django.core.mail.backends.smtp.EmailBackend' # Or 'django.core.mail.backends.console.EmailBackend'
EMAIL_HOST='smtp.gmail.com'
EMAIL_PORT=587
EMAIL_USE_TLS=True
EMAIL_HOST_USER='axaquizzies@gmail.com' # Your Gmail address
EMAIL_HOST_PASSWORD='uzkr znvw khcu xeid' # Your Gmail App Password

# Default email address to use for various automated emails
DEFAULT_FROM_EMAIL='your_email@gmail.com'
SERVER_EMAIL='your_email@gmail.com'

# Base URL for email links (used in tasks/signals.py)
BASE_URL='http://127.0.0.1:8000'
